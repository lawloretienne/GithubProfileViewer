<resources>

    <!-- Base application theme. -->
    <style name="Base.Theme.GithubProfileViewer" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorButtonNormal">@color/primary</item>
        <item name="colorControlActivated">@color/primary</item>
        <item name="colorControlHighlight">@color/grey_500</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>
        <item name="android:dialogTheme">@style/DialogTheme</item>
        <item name="android:alertDialogTheme">@style/AlertDialogTheme</item>
    </style>

    <style name="Theme.GithubProfileViewer" parent="Base.Theme.GithubProfileViewer" />

    <style name="Theme.GithubProfileViewer.ProfileActivity" parent="Theme.GithubProfileViewer" />

</resources>